/**
 * Input Component
 *
 * This is a convenience alias for FormGroup with `as="input"`.
 * For new code, prefer using FormGroup directly.
 *
 * @example
 * // These are equivalent:
 * <Input label="Email" name="email" type="email" />
 * <FormGroup label="Email" name="email" type="email" />
 */
export { default, default as Input } from "../ui-form-group/ui-form-group";
